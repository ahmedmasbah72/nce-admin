{
    "name": "nce-admin",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build && cp -r public dist",
        "dev": "vite",
        "vercel-build": "npm run build"
    },
    "devDependencies": {
        "vite": "^5.0.0",
        "laravel-vite-plugin": "^1.0"
    }
}